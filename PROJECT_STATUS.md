# 宗门志 — 项目进度与开发文档

## 一、项目简介

基于 SillyTavern Web 前端构建的修仙掌门决策类游戏。核心设计理念：**除剧情正文、天道运转、选项、近日要事外，其余全部信息由变量系统驱动**，支持前端操作与 LLM 输出双向更新，且按聊天存档独立持久化。

游戏不再预设固定宗门名称，玩家通过开局创建向导自定义宗门名称、修行法门、专长、困境等。

---

## 二、当前进展

### 已完成功能

| 模块 | 状态 | 说明 |
|------|------|------|
| **标题页与开场动画** | 已完成 | GSAP 驱动水墨山水卷轴展开动画，标题"宗门志" |
| **开局创建向导** | 已完成 | 四步 Wizard：玩家信息 → 宗门概况 → **弟子信息** → 确认，数据写入 gameState |
| **弟子信息配置** | 已完成 | 根据开局地位生成对应数量弟子槽位，逐个配置：姓名、性别、道号、境界、年龄、职务、天赋、忠诚度、性格标签、外貌标签；每个弟子带骰子随机按钮 |
| **天赋资质系统** | 已完成 | 甲乙丙丁 × 上中下（共 12 档），驱动角色三维计算 |
| **天机正文** | 已完成 | 支持富文本渲染：`"…"` 对话、`（…）` 心理活动、`《…》` 功法/秘籍等颜色区分 |
| **近日要事** | 已完成 | 事件卡片化，清晰展示时间 / 地点 / 人物 / 事件，`<sum>` 解析后持久化到 gameState.events |
| **变量系统（核心）** | 已完成 | Schema-first 设计，深度 Proxy 响应式，JSON Patch-like 操作（replace/delta/insert/remove），按 chat 持久化到 IndexedDB |
| **成员堂** | 已接入变量 | `members` 为 Record（key = 姓名），成员列表、详情弹窗、资源栏人数全部走变量；详情显示忠诚度、心情、性格标签、外貌标签 |
| **合纵阁（外交）** | 已接入变量 | `diplomacy` 为 Record（key = 势力名），卡片展示关系值与描述 |
| **山河殿（建筑/疆域）** | 已接入变量 | 新增导航与页面，含「山门建筑」「疆域舆图」两个标签页，读取 `world.buildings` 与 `world.regions`，支持锁定/解锁状态 |
| **资源栏自动刷新** | 已完成 | 灵石、威望、位阶、人数订阅变量变化，实时更新 |
| **LLM 流式解析** | 已完成 | 支持 `<maintext>`、`<option>`、`<thinking>`、`<sum>`、`<vars>`、`<var>` 标签捕获与处理 |
| **SillyTavern 集成** | 已完成 | 增强版 Prompt 组装、Lorebook 关键词匹配、上下文截断、Token 估算 |
| **数据持久化** | 已完成 | 对话级独立存档（IndexedDB），每个对话有独立的 gameState；自动存档 300ms 防抖 |
| **读档恢复** | 已完成 | 读档后自动刷新所有 UI 模块 |
| **读取游戏（标题页）** | 已完成 | 按玩家名称分组，每个角色 N 个存档槽，支持读取/删除/新建 |
| **开局提示词系统** | 已完成 | `isFirstInteraction()` 检测首次 LLM 交互，`buildOpeningPrompt()` 注入完整开局引导，包含玩家/宗门/弟子全部信息，要求 LLM 输出 `<vars>` 补充完整开局变量 |
| **序章正文展示** | 已完成 | `generateOpeningStory` 根据创建向导数据生成序章，展示宗门、掌门（含天赋）、弟子（含性格/外貌/忠诚度描述词）及开局困境 |
| **快照回滚** | 已完成 | 发送前 snapshot，API 失败时回滚 gameState、actionLogs、stamina、聊天记录 |
| **体力/操作记录** | 已完成 | 按 session 持久化，刷新页面不丢失；提交成功后清除 |
| **UI 响应式订阅修复** | 已完成 | player-realm 动态绑定、members subscribe 触发 renderMembers、diplomacy subscribe 触发 renderFactions |
| **天机推演面板** | 已完成 | top-bar 资源栏新增入口，含伏笔谱和天机动向两个标签页，支持增删改 |
| **伏笔系统** | 已完成 | `\u003cforeshadowing\u003e` 标签解析、倒计时管理、爆发时注入提示词 |
| **随机事件池** | 已完成 | 1D100 骰子、概率范围可编辑、全覆盖/无重叠/无空隙验证、6 个默认事件 |
| **天道推演面板增强** | 已完成 | 显示 `\u003cvars\u003e` 操作的人类可读效果，与 `\u003cthinking\u003e` 分区展示 |
| **LLM 输出格式规范** | 已完成 | `LLM_FORMAT_SPEC.md`（核心 prompt 注入）+ `LLM_REFERENCE.md`（参考查阅），含 `\u003canalysis\u003e` 分析模块 |
| **默认世界书** | 已完成 | 首次加载自动创建《宗门志》世界书，含"格式规范"和"变量规范"两个深度 0 条目，自动激活 |

### 部分完成 / 待接入变量

| 模块 | 状态 | 说明 |
|------|------|------|
| **任务系统** | Schema 已有，UI 待完善 | `quests.main` 与 `quests.side` 已定义，页面已有但待优化 |

### 尚未开始

| 模块 | 说明 |
|------|------|
| **战斗/探索系统** | 无页面与 Schema |
| **邮件/传讯系统** | 无页面与 Schema |
| **时间推进系统** | 目前事件依赖 LLM 驱动，未来可考虑加入回合/月份机制 |

---

## 三、开局创建向导

点击「开始新游戏」后进入四步创建流程，数据决定初始 gameState：

**第一步：玩家信息**
- 名字、境界（大境界+阶段）、性别、年龄
- 天赋资质（甲上~丁下）
- 性格标签、外貌标签

**第二步：宗门概况**
- 宗门名称（玩家自定义）
- 修行法门（道修/神修/魔修/体修/旁门左道）
- 宗门专长（剑/丹/炼器/法阵/符箓）
- 开局地位（末流小派 / 四流小宗 / 三流门派）
- 开局困境（临危受命/白手起家/仇敌环伺/天降机缘/宗门内乱）

**第三步：弟子信息**
- 根据开局地位自动生成对应数量弟子槽位
- 每个弟子可配置：姓名、性别、道号、境界、年龄、职务、天赋、忠诚度
- 性格标签（可多选）、外貌标签（可多选）
- 左侧骰子按钮可随机生成该弟子的所有字段（姓名不随机）
- 年龄受境界寿元上限约束

**第四步：确认**
- 汇总展示玩家、宗门、弟子全部信息
- 点击「开始游戏」生成初始状态并进入天机台

### 地位 → 初始数值映射

| 地位 | 灵石 | 弟子数 | 建筑 | 威望 |
|------|------|--------|------|------|
| 末流小派 | 3000 | 2 | 破旧大殿 | 100 |
| 四流小宗 | 5000 | 3 | 议事厅 + 修炼室 | 200 |
| 三流门派 | 8000 | 4 | 悟道殿 + 藏经阁 + 修炼室 | 400 |

### 弟子数据结构

```js
{
  id, name, daoName, realm, role, status,
  talent, color, stats, baseStats, lifespan,
  loyalty, mood,
  personality: [],  // 性格标签数组（新增）
  appearance: [],   // 外貌标签数组（新增）
  skills: [], equipment: []
}
```

---

## 四、天赋资质与三维计算

天赋分甲乙丙丁四等，每等分上中下三阶，共 12 档。角色三维（杀伐/防御/身法）由天赋和境界共同决定：

- **基准**：乙中 + 练气期前期 = 50
- **天赋差**：每高一小档 +3，每低一小档 −3
- **小境界**：前期→中期→后期→圆满，每阶 ×1.5
- **大境界**：练气→筑基→金丹→元婴→化神→道祖，每阶 ×10

公式：`Math.round((50 + talentDiff × 3) × 1.5^stage × 10^realm)`

---

## 五、项目地址与访问方式

- **GitHub 仓库**：`https://github.com/ariespo/zhangmen.git`
- **Vercel 部署控制台**：`https://vercel.com/klymds-projects/zhangmen`
- **线上预览地址**：`https://zhangmen.vercel.app/`

> 注：Vercel 已关联 GitHub 仓库，每次 `push` 到 `main` 分支会自动触发部署。

---

## 六、GitHub → Vercel 同步方法

### 方法 A：使用项目内的 `sync.sh`（推荐）

```bash
./sync.sh "提交说明"
```

### 方法 B：手动提交

```bash
git add .
git commit -m "你的提交说明"
git push origin main
```

---

## 七、关键文件说明

| 文件 | 职责 |
|------|------|
| `index.html` | 主界面、页面路由、UI 渲染、事件绑定、LLM 流式解析、开局创建向导（四步）、开场动画 |
| `sillytavern/game-state.js` | 变量系统核心：Schema DSL、GameStateManager（含 `reset()`）、Patch 应用、IndexedDB 持久化 |
| `sillytavern/st-prompt.js` | Prompt 组装、宏替换、历史截断、Token 估算、宗门状态格式化 |
| `sillytavern/st-engine.js` | Lorebook 关键词匹配引擎 |
| `sillytavern/st-core.js` | SillyTavern IndexedDB 封装与底层存储、对话 CRUD、导入/导出 |
| `sillytavern/st-integration.js` | SillyTavern Web 增强初始化、消息发送、全局 API 暴露 |
| `sillytavern/st-variables.js` | `<var>` 标签变量提取与注入 |
| `sillytavern/st-ui.js` | SillyTavern 通用 UI 组件与逻辑（世界书/预设/对话/设置/预览/变量面板） |

---

## 八、变量系统使用方式（给 LLM / 开发者）

LLM 在输出中可携带 `<vars>` 标签来更新游戏状态：

```xml
<vars>[{"op":"delta","path":"/finance/gold","value":"-500"},{"op":"replace","path":"/members/沈万钧/status","value":"闭关"}]</vars>
```

支持的 `op`：
- `replace`：替换值
- `delta`：对数字做加减
- `insert`：向数组或对象插入
- `remove`：从数组或对象删除

路径规则：以 `/` 分隔，Record 用 key（如 `/members/沈万钧`），Array 用索引（如 `/world/buildings/0/level`）。

---

## 九、近期关键变更

| 日期 | 变更 |
|------|------|
| 2026-04-18 | 天赋资质系统：甲乙丙丁×上中下，12 档天赋驱动三维计算 |
| 2026-04-18 | 开局创建向导：三步 Wizard，自定义玩家与宗门初始状态 |
| 2026-04-18 | 开场动画：GSAP 水墨山水卷轴展开 |
| 2026-04-18 | 项目标题改为「宗门志」，去除预设宗门名称 |
| 2026-04-19 | 硬编码数据全面迁移至变量系统：player、sect、events |
| 2026-04-19 | 资源栏、收支概况、宗门大阵、宗门实力等总览页数据改为变量驱动 |
| 2026-04-19 | 天机正文初始内容清空，改为空状态提示 |
| 2026-04-19 | 成员详情弹窗功法改为从成员 `skills` 数组读取 |
| 2026-04-19 | HTML title/meta 动态更新，随玩家/宗门名称变化 |
| 2026-04-19 | 创建向导改为四步，新增弟子信息配置页面；开局地位简化为 3 档 |
| 2026-04-19 | 弟子数据结构增加 personality、appearance 字段 |
| 2026-04-19 | 修复 recentEvents、stamina、actionLogs 不持久化 |
| 2026-04-19 | 修复读档后 UI 不刷新、snapshot 不回滚 gameState |
| 2026-04-19 | 修复 chat.messages 存储 LLM 原始标签污染历史 |
| 2026-04-19 | 添加 gameStateManager.reset()，新游戏时自动创建新存档并清除旧数据 |
| 2026-04-20 | 清除旧项目残留角色数据（沈万钧等），修复新游戏成员污染 |
| 2026-04-20 | 实装标题页「读取游戏」：按玩家名称分组，每个角色 N 个独立存档槽 |
| 2026-04-19 | 统一 `<var>` 和 `<vars>` 变量更新路径，读档不丢失 |
| 2026-04-20 | 测试按钮 `<vars>` 逐步完善：总览页/成员页/山河页/外交页 |
| 2026-04-20 | 修复 `.player-realm` 硬编码，改为从 `player.realm` 动态绑定 |
| 2026-04-20 | 修复 `/members` subscribe 漏掉 `renderMembers()`，成员列表现可实时刷新 |
| 2026-04-20 | 修复 `/diplomacy` subscribe 缺失，外交页面现可实时刷新 |
| 2026-04-20 | 移除总览页硬编码「灵脉灵资」版块 |
| 2026-04-20 | 明确角色三维由变量存储，LLM 修改境界时需同步更新 `baseStats` |
| 2026-04-21 | 新增「测试高级」按钮：测试 delta/remove 及未覆盖的 insert/replace 路径 |
| 2026-04-21 | 修复右上角 `.player-name`/`.player-realm` 硬编码，改为从 `player` 动态绑定 |
| 2026-04-21 | 新增 `LLM_FORMAT_SPEC.md`（核心 prompt 注入）和 `LLM_REFERENCE.md`（参考查阅） |
| 2026-04-21 | 天机推演系统：伏笔存储器 + `\u003cforeshadowing\u003e` 标签解析 + 倒计时归零注入 |
| 2026-04-21 | 随机事件池：1D100 掷骰、事件 CRUD、概率条可视化、全覆/无重叠/无空隙验证 |
| 2026-04-21 | 天道推演面板增强：`\u003cvars\u003e` JSON 转换为人类可读效果，与 `\u003cthinking\u003e` 分区展示 |
| 2026-04-21 | 格式规范新增 `\u003canalysis\u003e` 模块：要求 LLM 在 `\u003cvars\u003e` 前分析变量影响 |
| 2026-04-21 | 格式规范新增剧情结构要求：上次选择结果 + 时间流逝 + 弟子个性化 + 死亡机制 |
| 2026-04-21 | 首次加载自动创建默认世界书《宗门志》，导入格式规范和变量规范为深度 0 条目 |
| 2026-04-22 | **开局提示词系统**：`isFirstInteraction()` 检测首次 LLM 交互，`buildOpeningPrompt()` 注入完整开局引导，要求 LLM 通过 `<vars>` 补充功法/外交/宝库/机缘/事件/任务等变量 |
| 2026-04-22 | **序章正文补全**：`generateOpeningStory` 新增弟子信息（姓名、道号、境界、角色、天赋、忠诚度、性格、外貌）和掌门天赋；忠诚度映射为描述词 |
| 2026-04-22 | **设置面板角色同步**：userName 与 `player.name` 同步只读显示，AI 角色名硬编码为「宗门模拟器」 |
| 2026-04-22 | **高级测试增强**：mockText 加入 `<foreshadowing>` 标签并解析入库；伪装天机推演预处理输出伏笔爆发和随机事件日志 |
| 2026-04-22 | **修复反引号语法错误**：`buildOpeningPrompt` 模板字符串中嵌套反引号导致整段 `<script>` 无法执行，`switchPage` 等函数未定义 |
| 2026-04-22 | **验证持久化与回滚**：确认 events 增量追加 + IndexedDB 自动持久化，读档后正确恢复；LLM 请求失败时快照回滚覆盖 gameState/聊天记录/体力/操作记录 |

---

## 十、下一步建议（按优先级）

1. **手机端 UI 适配** ⭐ 最高优先级
   - 侧边导航栏改为底部 Tab 或抽屉式菜单
   - 资源栏横向滚动或折叠显示
   - 创建向导各步骤的表单元素适配小屏触控
   - 天机正文阅读体验优化（字号、行距、触控翻页）

2. **开局变量要求提示词优化**
   - 根据实际 LLM 输出效果，调整 `<vars>` 变量规范的表述方式
   - 优化变量数量/复杂度，避免 LLM 输出截断或遗漏
   - 考虑分批输出（先核心变量，后续剧情中再补充次要变量）

3. **外交、山河页面可玩性优化**
   - 外交：增加互动操作（拜访、送礼、结盟、宣战等），不只是静态展示
   - 山河：疆域舆图增加点击交互（查看区域详情、派遣弟子、开发资源）
   - 建筑：增加升级/拆除操作，与资源消耗挂钩

4. **弟子页面交互性优化**
   - 增加弟子操作（派遣任务、赏赐物品、指点修炼、逐出师门等）
   - 弟子间关系网络展示（好感度、恩怨）
   - 弟子成长时间线（境界突破记录、事件参与记录）
   - 批量操作或筛选排序功能

---

*文档更新时间：2026-04-22*
