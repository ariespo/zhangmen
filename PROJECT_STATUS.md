# 宗门志 — 项目进度与开发文档

## 一、项目简介

基于 SillyTavern Web 前端构建的修仙掌门决策类游戏。核心设计理念：**除剧情正文、天道运转、选项、近日要事外，其余全部信息由变量系统驱动**，支持前端操作与 LLM 输出双向更新，且按聊天存档独立持久化。

游戏不再预设固定宗门名称，玩家通过开局创建向导自定义宗门名称、修行法门、专长、困境等。

---

## 二、当前进展

### ✅ 已完成功能

| 模块 | 状态 | 说明 |
|------|------|------|
| **标题页与开场动画** | 已完成 | GSAP 驱动水墨山水卷轴展开动画，标题"宗门志" |
| **开局创建向导** | 已完成 | 三步 Wizard：玩家信息 → 宗门概况 → 确认，数据写入 gameState |
| **天赋资质系统** | 已完成 | 甲乙丙丁 × 上中下（共 12 档），驱动角色三维计算 |
| **天机正文** | 已完成 | 支持富文本渲染：`"…"` 对话、`（…）` 心理活动、`《…》` 功法/秘籍等颜色区分 |
| **近日要事** | 已完成 | 事件卡片化，清晰展示时间 / 地点 / 人物 / 事件 |
| **变量系统（核心）** | 已完成 | Schema-first 设计，深度 Proxy 响应式，JSON Patch-like 操作（replace/delta/insert/remove），按 chat 持久化到 IndexedDB |
| **成员堂** | 已接入变量 | `members` 为 Record（key = 姓名），成员列表、详情弹窗、资源栏人数全部走变量 |
| **合纵阁（外交）** | 已接入变量 | `diplomacy` 为 Record（key = 势力名），卡片展示关系值与描述 |
| **山河殿（建筑/疆域）** | 已接入变量 | 新增导航与页面，含「山门建筑」「疆域舆图」两个标签页，读取 `world.buildings` 与 `world.regions`，支持锁定/解锁状态 |
| **资源栏自动刷新** | 已完成 | 灵石、威望、位阶、人数订阅变量变化，实时更新 |
| **LLM 流式解析** | 已完成 | 支持 `<maintext>`、`<option>`、`<thinking>`、`<sum>`、`<vars>` 标签捕获与处理 |
| **SillyTavern 集成** | 已完成 | 增强版 Prompt 组装、Lorebook 关键词匹配、上下文截断、Token 估算 |

### ⚠️ 部分完成 / 待接入变量

| 模块 | 状态 | 说明 |
|------|------|------|
| **任务系统** | Schema 已有，UI 待完善 | `quests.main` 与 `quests.side` 已定义，页面已有但待优化 |

### ✅ 已接入变量系统（此前待完成）

| 模块 | 状态 | 说明 |
|------|------|------|
| **万宝阁（宝库）** | 已接入变量 | `treasury` 数据从 `gameState` 读取，支持分配/收回、实时刷新 |
| **藏经阁（功法）** | 已接入变量 | `library` 数据从 `gameState` 读取，订阅 `/library` 自动刷新 |
| **机遇** | 已接入变量 | `opportunities` 数据从 `gameState` 读取，支持标记完成、体力消耗 |

### 🚧 尚未开始

| 模块 | 说明 |
|------|------|
| **战斗/探索系统** | 无页面与 Schema |
| **邮件/传讯系统** | 无页面与 Schema |
| **时间推进系统** | 目前事件依赖 LLM 驱动，未来可考虑加入回合/月份机制 |

---

## 三、开局创建向导

点击「开始新游戏」后进入三步创建流程，数据决定初始 gameState：

**第一步：玩家信息**
- 名字、境界（大境界+阶段）、性别、年龄
- 天赋资质（甲上~丁下）
- 性格标签、外貌标签

**第二步：宗门概况**
- 宗门名称（玩家自定义，不再预设"云璃仙宗"）
- 修行法门（道修/神修/魔修/体修/旁门左道）
- 宗门专长（剑/丹/炼器/法阵/符箓）
- 开局地位（毫无名气 → 一流大宗）
- 开局困境（临危受命/白手起家/仇敌环伺/天降机缘/宗门内乱）

**第三步：确认**
- 汇总展示，点击「开始游戏」生成初始状态并进入天机台

### 地位 → 初始数值映射

| 地位 | 灵石 | 弟子数 | 建筑 | 威望 |
|------|------|--------|------|------|
| 毫无名气 | 500 | 0 | 0 | 0 |
| 末流小派 | 3000 | 1-2 | 1 | 100 |
| 三流小宗 | 6000 | 3 | 2 | 300 |
| 二流宗门 | 12580 | 6 | 3-4 | 800 |
| 一流大宗 | 30000 | 10 | 5+ | 1800 |

弟子最低职务为「内门弟子」。

---

## 四、天赋资质与三维计算

天赋分甲乙丙丁四等，每等分上中下三阶，共 12 档。角色三维（杀伐/防御/身法）由天赋和境界共同决定：

- **基准**：乙中 + 练气期前期 = 50
- **天赋差**：每高一小档 +3，每低一小档 −3
- **小境界**：前期→中期→后期→圆满，每阶 ×1.5
- **大境界**：练气→筑基→金丹→元婴→化神→道祖，每阶 ×10

公式：`Math.round((50 + talentDiff × 3) × 1.5^stage × 10^realm)`

---

## 五、项目地址与访问方式

- **GitHub 仓库**：`https://github.com/ariespo/zhangmen.git`
- **Vercel 部署控制台**：`https://vercel.com/klymds-projects/zhangmen`
- **线上预览地址**：`https://zhangmen.vercel.app/`

> 注：Vercel 已关联 GitHub 仓库，每次 `push` 到 `main` 分支会自动触发部署。

---

## 六、GitHub → Vercel 同步方法

### 方法 A：使用项目内的 `sync.sh`（推荐）

```bash
./sync.sh "提交说明"
```

### 方法 B：手动提交

```bash
git add .
git commit -m "你的提交说明"
git push origin main
```

---

## 七、关键文件说明

| 文件 | 职责 |
|------|------|
| `index.html` | 主界面、页面路由、UI 渲染、事件绑定、LLM 流式解析、开局创建向导、开场动画 |
| `sillytavern/game-state.js` | 变量系统核心：Schema DSL、GameStateManager、Patch 应用、IndexedDB 持久化 |
| `sillytavern/st-prompt.js` | Prompt 组装、宏替换、历史截断、Token 估算 |
| `sillytavern/st-engine.js` | Lorebook 关键词匹配引擎 |
| `sillytavern/st-core.js` | SillyTavern IndexedDB 封装与底层存储 |
| `sillytavern/st-integration.js` | SillyTavern Web 增强初始化、消息发送 |
| `sillytavern/st-ui.js` | SillyTavern 通用 UI 组件与逻辑 |

---

## 八、变量系统使用方式（给 LLM / 开发者）

LLM 在输出中可携带 `<vars>` 标签来更新游戏状态：

```xml
<vars>[{"op":"delta","path":"/finance/gold","value":"-500"},{"op":"replace","path":"/members/沈万钧/status","value":"闭关"}]</vars>
```

支持的 `op`：
- `replace`：替换值
- `delta`：对数字做加减
- `insert`：向数组或对象插入
- `remove`：从数组或对象删除

路径规则：以 `/` 分隔，Record 用 key（如 `/members/沈万钧`），Array 用索引（如 `/world/buildings/0/level`）。

---

## 九、近期关键变更

| 日期 | 变更 |
|------|------|
| 2026-04-18 | 天赋资质系统：甲乙丙丁×上中下，12 档天赋驱动三维计算 |
| 2026-04-18 | 开局创建向导：三步 Wizard，自定义玩家与宗门初始状态 |
| 2026-04-18 | 开场动画：GSAP 水墨山水卷轴展开 |
| 2026-04-18 | 项目标题改为「宗门志」，去除预设宗门名称 |
| 2026-04-19 | 硬编码数据全面迁移至变量系统：player（玩家信息）、sect（宗门信息）、events（近日要事） |
| 2026-04-19 | 资源栏、收支概况、宗门大阵、宗门实力等总览页数据改为变量驱动 |
| 2026-04-19 | 天机正文初始内容清空，改为空状态提示；选项按钮改为通用占位 |
| 2026-04-19 | 成员详情弹窗功法改为从成员 `skills` 数组读取 |
| 2026-04-19 | HTML title/meta 动态更新，随玩家/宗门名称变化 |

---

## 十、下一步建议（按优先级）

1. **测试 LLM 端到端效果** ⭐ 最高优先级
   - 跑一局完整对话，验证 `<vars>` 标签是否能正确修改状态并刷新 UI
   - 验证 player、sect、events 等新变量是否被 LLM 正确读写

2. **设计任务系统 UI**
   - 在总览页或新建「任务」页面展示主线阶段与支线列表

3. **考虑加入时间/回合机制**
   - 在 Schema 中加入 `world.turn` 或 `world.date`，让 LLM 可以推进游戏时间

4. **进一步优化空状态体验**
   - 当 player/sect 数据为空时，提供更友好的引导提示

---

*文档更新时间：2026-04-19*
